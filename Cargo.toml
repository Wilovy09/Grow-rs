[package]
name = "grow"
version = "0.1.0"
edition = "2021"
description = "🌱 Write sedders in RON"
categories = ["sqlx", "libsql", "cli", "database", "seeder"]
authors = ["Wilovy09"]
license = "MIT"
repository = "https://github.com/Wilovy09/Grow"
homepage = "https://github.com/Wilovy09/Grow"

[dependencies]
clap = { version = "4.5.23", features = ["derive", "string"] }
dotenv = "0.15.0"
tokio = { version = "1.42.0", features = ["full"] }
grow_libsql = { version = "0.1.0", path = "./grow_libsql" }
grow_sqlx = { version = "0.1.0", path = "./grow_sqlx" }

[profile.dist]
inherits = "release"
lto = "thin"
